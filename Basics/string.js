const name  = {"Naresh Singh"}
const repoCount = 30

console.log (name+repoCount)
console.log (`My name is ${name} and my repo is ${repoCount}`)

const gameName = new String("Naresh Singh")
console.log(gameName[0])
console.log(gameName)

#naresh Singh 
